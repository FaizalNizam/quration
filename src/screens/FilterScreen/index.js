import React, {useEffect, useLayoutEffect} from 'react';
import FilterScreen from './FilterScreen';

const FilterScreenContainer = props => {
  return <FilterScreen {...props} />;
};
export default FilterScreenContainer;
