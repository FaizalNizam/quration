import React from 'react'
import FeaturedScreen from './FeaturedScreen'

const FeaturedScreenContainer = (props) => {
    return (
        <FeaturedScreen {...props} />
    )
}
export default FeaturedScreenContainer
