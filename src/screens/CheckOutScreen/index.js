import React from 'react';
import CheckOutScreen from './CheckOutScreen';

const CheckOutScreenContainer = props => {
  return <CheckOutScreen {...props} />;
};
export default CheckOutScreenContainer;
